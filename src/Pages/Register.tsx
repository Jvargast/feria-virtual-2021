import React, { useState } from 'react'
//import { onRegister } from '../components/Auth.api';

export default function () {

    /*const[{username, password, repeatPassword,firstName, lastName, email,phone,address, condominium, rol}, setRegisterData] = useState({
        username:'',
        password:'',
        repeatPassword:'',
        firstName:'',
        lastName:'',
        email:'',
        phone:'',
        address:'',
        condominium:'',
        rol:''


    })
    const [error, setError] = useState('')
    const register =async (event: React.FormEvent)=> {
        event?.preventDefault();
        if(password === repeatPassword) {
            //Perform API call
            const response = await onRegister({
                username,
                password

            })
            
            if(response && response.error) {
                setError(response.error);
            }


        } else {
             setError('password and repeat password must be the same')
        }
    }*/
    return (
        <div>
            Register
            
        </div>
    )
}
